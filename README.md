### ansible
# harvester
sudo apt-get update
sudo apt-get install python3-pip
pip3 install couchdb
pip3 install textblob
pip3 install geopy
sudo apt install mpich
pip3 install mpi4py
pip3 install vaderSentiment

python3 processParallelTweets.py
